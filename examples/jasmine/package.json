{
  "name": "jasmine-tests",
  "scripts": {
    "test": "dotenvx run -f ../../.env -- jasmine --reporter=@buddy-works/unit-tests/jasmine"
  },
  "devDependencies": {
    "@dotenvx/dotenvx": "1.49.0",
    "jasmine": "latest"
  },
  "dependencies": {
    "@buddy-works/unit-tests": "file:../../unit-tests.tgz"
  }
}
