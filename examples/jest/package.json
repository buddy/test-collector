{
  "name": "jest-tests",
  "scripts": {
    "test": "jest",
    "test:collect": "jest --reporters=default --reporters=test-collector/jest --testMatch='**/test/**/*.test.js'"
  },
  "devDependencies": {
    "jest": "30.0.5"
  },
  "dependencies": {
    "axios": "1.10.0",
    "test-collector": "file:../../test-collector.tgz"
  }
}
